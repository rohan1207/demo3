VITE_API_BASE=http://localhost:5000/api
VITE_RAZORPAY_KEY_ID=rzp_test_RjxqmNyYCFtFU0

# Sequence frames: default is same-origin (commit public/assets/seq/ — Render serves them with the site; no CDN needed).
# Only set this if you mirror frames elsewhere (optional; ~1600 PNGs on a third-party CDN can be costly).
#VITE_SEQUENCE_BASE=https://example.com
